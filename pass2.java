//includes pass 1 location and calculated object code
public class pass2 extends intermediate {
    /*format?? could probably work around labeling this. Just use case switch logic...
        All flags should be set before this. This function will just contain the formulas for each format
        Format 2: Register to register string contains %RA, and ,%RA load all combinations
        Format 3: Default
        Format 4: if mnmonic contains +
        Immediate: if operand contains #
     */

}
